# 21.3 не запускается на Python 3.13 и новее, а дома стоит 3.14. Версия 22
# работает и там, и на сервере с 3.11 - так везде один и тот же код.
python-telegram-bot[socks]==22.8
httpx[socks]==0.27.0
beautifulsoup4==4.12.3
# Повторяет рукопожатие настоящего Chrome. Без него Авито за защитой Qrator
# отдаёт капчу: httpx выдаёт себя не заголовками, а самим соединением.
curl_cffi==0.16.0
